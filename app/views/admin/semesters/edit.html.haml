%h1 Edit semester

= form_for [:admin, @university, @faculty, @specialty, @group, @semester] do |f|
  = f.label ' Semester number'
  = f.select :sem_number, options_for_select: [1, 2, 3, 4, 5, 6, 7]
  = f.submit 'Submit'
